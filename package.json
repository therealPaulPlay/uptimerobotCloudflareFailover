{
  "name": "serverswitcher",
  "version": "1.0.0",
  "description": "This server for OpenGuessr uses an API to monitor the availability of servers and switches to backup servers if necessary.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7",
    "node-cron": "^3.0.3"
  }
}
